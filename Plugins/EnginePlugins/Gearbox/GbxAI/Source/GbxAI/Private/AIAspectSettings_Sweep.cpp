#include "AIAspectSettings_Sweep.h"

FAIAspectSettings_Sweep::FAIAspectSettings_Sweep() {
    this->SweepRadius = 0.00f;
    this->SweepAngle = 0.00f;
    this->SweepAngleInterval = 0.00f;
    this->Direction = ESweepDirection::Left;
    this->SearchRadius = 0.00f;
}

